# Руководство администратора Telegram бота

## Что это за бот?

Этот бот автоматически отправляет ежедневные материалы из ваших программ пользователям в Telegram. Бот берет контент с Яндекс.Диска и доставляет его по расписанию или по запросу.

**Что нужно для работы:**
- Компьютер с Windows
- Установленный Python (версия 3.7 или выше)
- Аккаунт Яндекс.Диска
- Аккаунт в Telegram
- Токен Telegram бота (получается через @BotFather)
- Токен API Яндекс.Диска

---

## Подготовка: получение токенов

### 1. Получение токена Telegram бота

**Шаг 1:** Откройте Telegram и найдите бота **@BotFather**

**Шаг 2:** Отправьте команду `/newbot` боту @BotFather

**Шаг 3:** @BotFather попросит вас придумать имя для бота (например: "Мой учебный бот")

**Шаг 4:** Затем придумайте username для бота (должен заканчиваться на `bot`, например: `my_learning_bot`)

**Шаг 5:** @BotFather отправит вам **токен бота** - это длинная строка вида:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ ВАЖНО:** Сохраните этот токен! Он понадобится для настройки бота. Без него бот не будет работать.

### 2. Получение токена API Яндекс.Диска

Для получения OAuth-токена Яндекс.Диска следуйте официальной инструкции:

📖 **[Получение OAuth-токенов для Яндекс.Диска](https://yandex.ru/dev/disk-api/doc/ru/concepts/quickstart#quickstart__oauth)**

**⚠️ ВАЖНО:** Сохраните полученный токен! Он нужен для доступа к файлам на вашем Яндекс.Диске. Токен имеет вид строки, например: `y0_AgAAAAAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## Установка и настройка

### Шаг 1: Установка Python

Если Python еще не установлен:

1. Скачайте Python с сайта: https://www.python.org/downloads/
2. При установке **обязательно** отметьте галочку "Add Python to PATH"
3. Завершите установку

### Шаг 2: Подготовка файлов бота

1. Распакуйте все файлы бота в одну папку (например: `C:\bot_for_psych\`)
2. Откройте эту папку

### Шаг 3: Создание файла настроек

В папке с ботом найдите файл **`settings.ini`** и откройте его в текстовом редакторе (Блокнот).

Файл должен выглядеть так:
```ini
[tokens]
bot_token = 
disk_token = 

[bot]
cache_chat_id = 

[scheduler]
delivery_time = 09:00
```

**Заполните токены:**

1. Вставьте токен Telegram бота после `bot_token = ` (без пробелов)
2. Вставьте токен Яндекс.Диска после `disk_token = ` (без пробелов)
3. Установите время доставки (например, `09:00` означает 9 часов утра)
4. Сохраните файл

**Пример заполненного файла:**
```ini
[tokens]
bot_token = 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
disk_token = y0_AgAAAAAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

[bot]
cache_chat_id = 

[scheduler]
delivery_time = 09:00
```

---

## Структура папок на Яндекс.Диске

Бот ищет программы (папки) в **папке bot** вашего Яндекс.Диска.

### Организация программ

Каждая программа - это отдельная папка в папке bot диска. Например:
- `program_1` - первая программа
- `program_2` - вторая программа
- `Название программы` - можно использовать любое название

### Папка "present" (публичные материалы)

В корне вашего Яндекс.Диска может быть специальная папка **`present`** для публичных материалов, доступных через глубокие ссылки (deep links).

**Особенности папки present:**
- Доступна **без регистрации** - любой пользователь может открыть материалы по ссылке
- Не требует username у пользователя
- Используется для публичного распространения контента

**Структура папки present:**

```
present/
├── msg.txt                    (текстовое сообщение для корневой папки)
├── option1/                   (подпапка)
│   ├── msg.txt                (текстовое сообщение для этой папки)
│   └── option1_1/             (вложенная подпапка)
│       └── msg.txt
└── option2/                   (другая подпапка)
    └── msg.txt
```

**Как это работает:**
1. В каждой папке может быть файл `msg.txt` с текстовым сообщением (поддерживает HTML-разметку)
2. Пользователи видят сообщение из `msg.txt` и список доступных подпапок
3. Пользователи могут навигировать по папкам, нажимая на кнопки с названиями папок
4. Кнопка "◀️ Назад" позволяет вернуться к предыдущей папке

**Создание структуры:**
1. Создайте папку `present` в папке bot Яндекс.Диска
2. Внутри создайте подпапки с любыми названиями
3. В каждой папке (включая корневую `present`) можно разместить файл `msg.txt` с текстом
4. Текст в `msg.txt` поддерживает HTML-разметку (жирный, курсив, ссылки и т.д.)

**Пример файла msg.txt:**
```
<b>Добро пожаловать!</b>

<i>Выберите интересующий вас раздел:</i>

✅ Раздел 1
✅ Раздел 2

Посетите наш <a href="https://example.com">сайт</a> для дополнительной информации.
```

### Структура папки программы

Внутри каждой папки программы должны быть папки с днями:
```
program_1/
├── 1_day/          (папка для первого дня)
├── 2_day/          (папка для второго дня)
├── 3_day/          (папка для третьего дня)
└── ...
```

**Важно:** Название папки дня должно быть в формате `N_day`, где N - номер дня (1, 2, 3, и т.д.)

### Содержимое папки дня

В каждой папке дня (`1_day`, `2_day`, и т.д.) вы можете размещать:

1. **Текстовый файл** (`.txt`) - основное сообщение дня
   - Можно использовать HTML-разметку (жирный текст, курсив, ссылки)
   - Можно использовать эмодзи
   - Пример имени: `message.txt` или `день1.txt`

2. **Изображения** - фотографии, картинки
   - Форматы: `.jpg`, `.jpeg`, `.png`, `.gif`, и другие

3. **Видео** - видеофайлы
   - Форматы: `.mp4`, `.avi`, `.mov`, и другие

4. **Аудио** - аудиофайлы
   - Форматы: `.mp3`, `.wav`, `.ogg`, и другие

5. **Документы** - документы для скачивания
   - Форматы: `.doc`, `.docx`, `.pdf`

**Пример структуры папки дня:**
```
1_day/
├── message.txt          (текстовое сообщение)
├── photo.jpg           (изображение)
├── video.mp4           (видео)
└── document.pdf        (документ)
```

### Форматирование текста в .txt файлах

Вы можете использовать HTML-теги для оформления текста:

- **Жирный текст:** `<b>жирный</b>` или `<strong>жирный</strong>`
- *Курсив:* `<i>курсив</i>` или `<em>курсив</em>`
- <u>Подчеркнутый:</u> `<u>подчеркнутый</u>`
- ~~Зачеркнутый:~~ `<s>зачеркнутый</s>`
- `Код:` `<code>код</code>`
- [Ссылка](URL): `<a href="https://example.com">ссылка</a>`

**Пример файла message.txt:**
```
<b>День 1: Начало программы</b>

<i>Сегодня мы начинаем изучение основных концепций.</i>

✅ Задачи на сегодня:
• Изучить материалы
• Выполнить практику

Посетите наш <a href="https://example.com">сайт</a> для дополнительной информации.
```

---

## Глубокие ссылки (Deep Links) и папка Present

### Что такое глубокие ссылки?

Глубокие ссылки (deep links) - это специальные ссылки, которые открывают бота и сразу показывают определенный контент из папки `present`. Они позволяют предоставить публичный доступ к материалам без необходимости регистрации пользователя.

### Формат глубоких ссылок

Ссылки имеют следующий формат:
```
https://t.me/your_bot_name?start=p=путь
```

Где:
- `your_bot_name` - имя вашего бота (например: `my_learning_bot`)
- `p=` - обязательный параметр для доступа к папке present
- `путь` - относительный путь внутри папки present (оставить пустым, если нужны все варианты)

### Примеры ссылок

**Корневая папка present:**
```
https://t.me/my_learning_bot?start=p=
```

**Папка "option1" внутри present:**
```
https://t.me/my_learning_bot?start=p=option1
```

**Вложенная папка "option1/option2":**
```
https://t.me/my_learning_bot?start=p=option1/option2
```

### Как создать глубокую ссылку

1. **Узнайте имя вашего бота:**
   - Откройте Telegram
   - Найдите вашего бота (например, через @BotFather)
   - Имя бота будет в формате `@bot_name`

2. **Создайте ссылку:**
   - Для корневой папки: `https://t.me/bot_name?start=p=`
   - Для конкретной папки: `https://t.me/bot_name?start=p=название_папки`
   - Для вложенной папки: `https://t.me/bot_name?start=p=папка1/папка2`

3. **Поделитесь ссылкой:**
   - Отправьте ссылку пользователям через любой канал (сообщение, сайт, социальные сети)
   - Пользователи могут нажать на ссылку, и бот откроется с нужным контентом

### Настройка папки Present

**Шаг 1: Создайте папку на Яндекс.Диске**

1. Зайдите на Яндекс.Диск
2. Создайте папку с названием **`present`** в папке bot диска
3. Эта папка будет использоваться для публичных материалов

**Шаг 2: Создайте структуру папок**

Внутри папки `present` создайте нужную структуру:
- Подпапки с любыми названиями
- Вложенные подпапки (любая глубина вложенности)

**Шаг 3: Добавьте текстовые сообщения**

В каждой папке (включая корневую `present`) можно создать файл `msg.txt`:
- Этот файл будет отображаться пользователям при открытии папки
- Поддерживает HTML-разметку (жирный, курсив, ссылки и т.д.)
- Если файла нет, будет показан значок папки 📁

**Пример структуры:**
```
present/
├── msg.txt                    (сообщение для корневой папки)
├── welcome/                   (подпапка "welcome")
│   ├── msg.txt                (сообщение для папки welcome)
│   └── materials/             (вложенная папка)
│       └── msg.txt
└── courses/                   (подпапка "courses")
    └── msg.txt
```

**Шаг 4: Создайте ссылки**

Для каждой папки создайте соответствующую ссылку:
- Корневая: `https://t.me/bot_name?start=p=`
- Папка welcome: `https://t.me/bot_name?start=p=welcome`
- Вложенная папка: `https://t.me/bot_name?start=p=welcome/materials`

### Особенности работы с Present

**Безопасность:**
- Доступ ограничен только папкой `present` и её подпапками
- Пользователи не могут выйти за пределы этой папки
- Попытки доступа к другим папкам блокируются

**Навигация:**
- Пользователи видят список подпапок в виде кнопок
- Кнопки расположены по 2 в ряд для удобства
- Кнопка "◀️ Назад" появляется автоматически, если есть родительская папка
- Навигация работает через callback-кнопки (не требует ввода текста)

**Регистрация:**
- **Не требуется** для доступа к папке present
- Пользователи могут открывать материалы без username
- Это полностью публичный доступ

**Использование:**
- Распространение материалов через социальные сети
- Размещение ссылок на сайте
- Отправка ссылок в сообщениях
- QR-коды со ссылками для печатных материалов

### Примеры использования

**Пример 1: Приветственная страница**
1. Создайте `present/msg.txt` с приветственным сообщением
2. Создайте подпапки: `about`, `courses`, `contact`
3. Создайте ссылку: `https://t.me/bot_name?start=p=`
4. Поделитесь ссылкой - пользователи увидят приветствие и список разделов

**Пример 2: Многоуровневая навигация**
1. Создайте структуру: `present/courses/programming/python/`
2. В каждой папке добавьте `msg.txt` с описанием
3. Создайте ссылку для конкретного раздела: `https://t.me/bot_name?start=p=courses/programming/python`
4. Пользователи смогут навигировать по всей структуре

**Пример 3: Разные варианты доступа**
- Общая ссылка на корень: `https://t.me/bot_name?start=p=`
- Ссылка на конкретный раздел: `https://t.me/bot_name?start=p=section1`
- Разные ссылки для разных аудиторий

### Важные замечания

✅ **Папка present должна быть в папке bot диска** - бот ищет её по пути `bot/present`

✅ **Используйте понятные названия папок** - они будут видны пользователям как кнопки

✅ **Файл msg.txt опционален** - если его нет, будет показан значок папки

✅ **HTML-разметка поддерживается** - можно использовать теги `<b>`, `<i>`, `<a>`, и т.д.

✅ **Ссылки работают сразу** - после создания папки и файлов на диске, ссылки начинают работать автоматически

✅ **Безопасность** - пользователи не могут получить доступ к другим папкам на диске, только к present

---

## Запуск бота

### Способ 1: Запуск через графический интерфейс (рекомендуется)

1. В папке с ботом найдите файл **`launch_ui.bat`**
2. Дважды щелкните по этому файлу
3. Откроется окно программы для управления ботом

**Что делает `launch_ui.bat`:**
- Автоматически создает виртуальное окружение (если нужно)
- Устанавливает необходимые программы
- Запускает графический интерфейс

### Способ 2: Запуск из командной строки

1. Откройте командную строку (нажмите Win+R, введите `cmd`, нажмите Enter)
2. Перейдите в папку с ботом:
   ```
   cd C:\bot_for_psych
   ```
   (замените путь на путь к вашей папке)
3. Запустите команду:
   ```
   python run_ui.py
   ```

---

## Использование графического интерфейса

### Главное окно

После запуска откроется окно с несколькими разделами:

#### 1. Раздел "Настройки токенов"

Здесь вы можете:
- **Токен бота** - указать или изменить токен Telegram бота
- **Токен Яндекс.Диска** - указать или изменить токен Яндекс.Диска

**Как сохранить настройки:**
1. Заполните все поля
2. Нажмите кнопку **"Сохранить"** (в разделе "Настройки токенов")
3. Появится сообщение "Настройки сохранены"

**Как загрузить настройки из файла:**
- Нажмите кнопку **"Загрузить"** (в разделе "Настройки токенов")
- Настройки загрузятся из файла `settings.ini`

#### 2. Раздел "Управление ботом"

Здесь вы можете:
- **Запустить бота** - нажмите кнопку **"Запустить бота"**, чтобы запустить бота
- **Статус работы** - отображается рядом с кнопкой (бот запущен / бот остановлен)
- **Время доставки** - установить время, когда бот будет отправлять материалы ежедневно
  - Введите время в формате `ЧЧ:ММ` (например: `09:00` для 9 утра)
  - Нажмите кнопку **"Обновить"** рядом с полем времени
  - Время сохранится автоматически

**⚠️ ВАЖНО:** Бот должен быть запущен постоянно, чтобы отправлять материалы пользователям! Чтобы остановить бота, закройте окно программы.

#### 3. Раздел "Пользователи и программы"

Здесь отображается структура программ и пользователей в виде дерева.

**Структура:**
```
📁 program_1 (дата начала: 2025-12-24)
  ├── @username1 (Имя пользователя)
  ├── @username2 (Имя пользователя)
  └── ➕ Добавить пользователя
```

### Загрузка программ с диска

Чтобы автоматически загрузить программы, которые уже есть на вашем Яндекс.Диске:

1. Убедитесь, что токен Яндекс.Диска указан и сохранен
2. Нажмите кнопку **"Загрузить программы с диска"** (или похожую кнопку)
3. Программа найдет все папки в папке bot диска
4. Для каждой программы вас попросят указать **дату начала** в формате `YYYY-MM-DD` (например: `2025-12-24`)
5. Программы появятся в дереве пользователей

### Добавление пользователя

**Важно:** Пользователь должен иметь **username** в Telegram (не путать с именем в профиле!).

**Как добавить пользователя:**

1. Найдите программу в дереве (например: `📁 program_1`)
2. Дважды щелкните на строку **"➕ Добавить пользователя"** под этой программой
3. Откроется окно, в котором нужно ввести:
   - **Username** (например: `@username` или просто `username` - символ @ добавится автоматически)
   - **Имя** (необязательно - это имя будет видно в сообщениях бота)
4. Нажмите кнопку **"Добавить"**
5. **Важно:** Нажмите кнопку **"Сохранить изменения"** в разделе "Управление пользователями", чтобы сохранить изменения

**Как узнать username пользователя:**
- Попросите пользователя открыть Telegram
- Настройки → Редактировать профиль → Имя пользователя
- Если username не установлен, пользователь должен его установить

### Редактирование пользователя

1. Дважды щелкните по username пользователя в дереве
2. Вы можете изменить:
   - Имя пользователя (как его будут видеть в сообщениях)
   - Дата начала программы (если нужно изменить)
3. Нажмите "Сохранить"

### Удаление пользователя

1. Выберите пользователя в дереве (щелкните по нему один раз)
2. Нажмите правой кнопкой мыши
3. В появившемся меню выберите **"Удалить"**
4. **Важно:** Нажмите кнопку **"Сохранить изменения"** в разделе "Управление пользователями", чтобы сохранить изменения

### Сохранение изменений

**⚠️ ВАЖНО:** После любых изменений (добавление, редактирование, удаление пользователей или программ) вы должны нажать кнопку **"Сохранить изменения"** в разделе "Управление пользователями". Без этого изменения не будут сохранены!

---

## Файл handler_list.Json

Этот файл хранит информацию о программах и пользователях. Обычно он редактируется через графический интерфейс, но вы можете открыть его в текстовом редакторе для просмотра.

**Структура файла:**
```json
{
    "program_1": {
        "begin_date": "2025-12-24",
        "@username1": {
            "name": "Иван Иванов",
            "chat_id": 123456789,
            "last_message_date": 1734998400
        },
        "@username2": {
            "name": "Мария Петрова",
            "chat_id": 987654321,
            "last_message_date": null
        }
    },
    "program_2": {
        "begin_date": "2025-01-01"
    }
}
```

**Не редактируйте этот файл вручную**, если вы не уверены в том, что делаете! Используйте графический интерфейс.

---

## Настройка даты начала программы

Дата начала программы (`begin_date`) - это дата, с которой начинается отсчет дней.

**Как это работает:**
- День 1 = дата начала программы
- День 2 = дата начала + 1 день
- День 3 = дата начала + 2 дня
- И так далее

**Пример:**
Если программа начинается 24 декабря 2025 года (`2025-12-24`):
- 24 декабря = День 1
- 25 декабря = День 2
- 26 декабря = День 3

**Важно:** Материалы для дня станут доступны только когда наступит этот день. Например, если сегодня 24 декабря, пользователи увидят только День 1, даже если на диске есть папки `2_day`, `3_day` и т.д.

---

## Расписание доставки

Бот автоматически отправляет материалы пользователям каждый день в указанное время.

**Настройка времени:**
1. В графическом интерфейсе найдите раздел "Управление ботом"
2. Найдите поле "Время доставки"
3. Введите время в формате `ЧЧ:ММ` (например: `09:00` для 9 утра)
4. Нажмите кнопку **"Обновить"** рядом с полем времени
5. Если бот уже запущен, время применится автоматически. Если нет - время будет применено при следующем запуске

**Что происходит:**
- Каждый день в указанное время бот проверяет, какой день программы сейчас
- Бот отправляет материалы этого дня всем пользователям программы
- Пользователи получают все файлы из папки этого дня

---

## Часто задаваемые вопросы

### ❓ Бот не запускается, что делать?

**Проверьте:**
1. Указаны ли токены в `settings.ini`?
2. Правильно ли указаны токены (без лишних пробелов)?
3. Установлен ли Python и добавлен ли он в PATH?
4. Установлены ли все необходимые программы? Попробуйте запустить `launch_ui.bat` снова

### ❓ Пользователи не получают материалы

**Проверьте:**
1. Запущен ли бот? (Статус должен показывать, что бот работает)
2. Правильно ли указана дата начала программы?
3. Есть ли папки с днями на Яндекс.Диске? (например: `1_day`, `2_day`)
4. Есть ли файлы в папках дней?
5. Правильный ли токен Яндекс.Диска? (проверьте доступ к диску)

### ❓ Как добавить материалы для нового дня?

1. Зайдите на Яндекс.Диск
2. Откройте папку программы (например: `program_1`)
3. Создайте новую папку с названием `N_day`, где N - номер дня (например: `5_day` для 5-го дня)
4. Загрузите файлы в эту папку
5. Материалы автоматически станут доступны пользователям в нужный день

### ❓ Как изменить время доставки?

1. Откройте графический интерфейс
2. Измените время в поле "Время доставки"
3. Нажмите "Сохранить настройки"
4. Перезапустите бота

### ❓ Можно ли отправить материалы вручную?

Пользователи могут запросить материалы вручную через кнопку "📅 Выбрать день" в боте. Бот покажет все доступные дни, и пользователь сможет выбрать нужный.

### ❓ Что делать, если пользователь не видит программы?

**Проверьте:**
1. Добавлен ли пользователь в программу?
2. Правильный ли username у пользователя? (должен начинаться с @)
3. Попросите пользователя написать `/start` боту
4. Убедитесь, что пользователь введет имя при первом запуске

### ❓ Как создать глубокую ссылку для папки present?

1. Узнайте имя вашего бота (например: `@my_bot`)
2. Определите путь к папке внутри `present` (например: `option1` или `folder1/subfolder`)
3. Создайте ссылку в формате: `https://t.me/my_bot?start=p=путь`
4. Для корневой папки present используйте: `https://t.me/my_bot?start=p=`

### ❓ Пользователи не видят содержимое папки present

**Проверьте:**
1. Существует ли папка `present` в папке bot Яндекс.Диска?
2. Правильно ли указан путь в ссылке? (проверьте, что папка существует)
3. Есть ли файл `msg.txt` в папке? (если нет, будет показан только значок папки)
4. Правильно ли указано имя бота в ссылке? (должно совпадать с реальным именем)

### ❓ Как изменить структуру папки present?

1. Зайдите на Яндекс.Диск
2. Откройте папку `present`
3. Добавьте, удалите или переименуйте подпапки
4. Добавьте или измените файлы `msg.txt` в нужных папках
5. Изменения вступят в силу сразу - пользователи увидят обновленную структуру

### ❓ Бот показывает ошибку при доступе к диску

**Проверьте:**
1. Правильный ли токен Яндекс.Диска?
2. Не истек ли срок действия токена? (если истек, получите новый)
3. Есть ли доступ к интернету?

---

## Важные напоминания

✅ **Бот должен работать постоянно** - чтобы отправлять материалы, бот должен быть запущен на компьютере. Если вы выключите компьютер или закроете программу, бот перестанет работать.

✅ **Регулярно проверяйте логи** - если бот показывает ошибки, проверьте окно программы на наличие сообщений об ошибках.

✅ **Делайте резервные копии** - периодически сохраняйте файл `handler_list.Json`, чтобы не потерять информацию о пользователях.

✅ **Проверяйте структуру диска** - убедитесь, что папки дней названы правильно (`1_day`, `2_day`, и т.д.)

✅ **Используйте графический интерфейс** - он проще и безопаснее, чем редактирование файлов вручную.

---

## Структура файлов проекта

Основные файлы и папки:

- **`settings.ini`** - файл с настройками (токены, время доставки)
- **`handler_list.Json`** - список программ и пользователей
- **`launch_ui.bat`** - файл для запуска графического интерфейса (дважды щелкните)
- **`run_ui.py`** - запуск графического интерфейса из командной строки
- **`run_bot.py`** - запуск бота из командной строки (без интерфейса)
- **`bot/`** - папка с кодом бота
- **`ui/`** - папка с кодом графического интерфейса

**Не удаляйте и не перемещайте эти файлы!**

---

## Краткая памятка

1. **Получите токены:** Telegram бота (@BotFather) и Яндекс.Диска (oauth.yandex.ru)
2. **Настройте `settings.ini`:** вставьте токены и установите время доставки
3. **Подготовьте структуру на диске:** создайте папки программ и дней
4. **Создайте папку present (опционально):** для публичных материалов через глубокие ссылки
5. **Запустите `launch_ui.bat`:** откроется графический интерфейс
6. **Загрузите программы с диска:** программа найдет папки автоматически
7. **Добавьте пользователей:** укажите их username в Telegram
8. **Создайте глубокие ссылки:** для доступа к папке present (если используется)
9. **Запустите бота:** нажмите кнопку "Запустить бота"
10. **Проверяйте работу:** убедитесь, что бот работает и отправляет материалы

**Желаем успешной работы с ботом!** 🎉

